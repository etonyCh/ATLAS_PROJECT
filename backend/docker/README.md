Backend container assets for the ATLAS platform.

Files in this directory provide backend-local container definitions so the backend
module satisfies the specification layout even though the main multi-service
orchestration entrypoint still lives at the repository root.

