"""
Task package for async workers and background jobs.

Legacy Celery entrypoints still live under `services/`. This package exists so the
backend layout matches the ATLAS specification while the task code is migrated.
"""

