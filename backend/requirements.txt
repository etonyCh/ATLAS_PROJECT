--extra-index-url https://download.pytorch.org/whl/cpu

# ============================================================================
# ATLAS API Core — Dependency Manifest
# ============================================================================


# ── WEB FRAMEWORK & CONFIG ──
fastapi==0.135.2
uvicorn==0.42.0
python-multipart==0.0.20
pydantic==2.12.5
pydantic-settings==2.13.1
email-validator==2.3.0
starlette==1.0.0
fastapi-limiter==0.1.5

# ── DATABASE & CACHE ──
SQLAlchemy==2.0.48
sqlmodel==0.0.37
asyncpg==0.31.0
alembic==1.18.4
redis==4.6.0

# ── AUTH & CRYPTO ──
passlib==1.7.4
bcrypt==5.0.0
python-jose==3.5.0
pyotp==2.9.0
argon2-cffi==25.1.0

# ── UTILITIES ──
httpx==0.28.1
python-dotenv==1.2.2
resend==2.26.0
structlog==25.5.0

# ============================================================================
# PROTOBUF / GRPC ANCHORS (Stable < 5.0 versions to fix pip loops)
# ============================================================================
grpcio==1.62.2
protobuf==4.25.3
googleapis-common-protos==1.63.2

# ============================================================================
# OMNI-ARCHITECT PIPELINE (Zero Local ML)
# ============================================================================

# ── DATABASES ──
qdrant-client==1.12.0
neo4j>=5.14.0

# ── GRAPH KNOWLEDGE ENGINE ──
lightrag-hku>=1.0.1
networkx>=3.0.0

# ── DOCUMENT PROCESSING & RAG ──
docling>=1.0.0
tiktoken==0.12.0
pypdf>=4.0.0
pymupdf>=1.23.0
onnxruntime>=1.17.0
fastembed>=0.2.0


# ── LLM & INFERENCE ENGINE ──
litellm>=1.30.0
google-generativeai>=0.4.0

# ── CLI / LOGGING ──
rich==14.3.3

# ============================================================================
# TESTING & DEV
# ============================================================================
pytest==9.0.2
pytest-asyncio==1.3.0

psycopg2-binary

meilisearch